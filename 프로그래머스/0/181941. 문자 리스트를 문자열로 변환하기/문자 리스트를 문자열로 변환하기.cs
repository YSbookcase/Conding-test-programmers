using System;

public class Solution {
    public string solution(string[] arr) {
        
        string answer = "";
        
        answer = string.Join("", arr);
        
//         foreach(string s in arr)
//         {
//             answer += s;
//         }
        
        return answer;
    }
}